"""Conditions C1-C5: per-condition system-prompt overlays for E1 (CLAUDE.md §4).

In Phase 1 the prompt templates are stored as data under ``templates/`` and only the
overlay-selection logic is unit-tested — no model is called. Implemented in Milestone 9.
"""
